{
  "name": "vision",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc",
    "postbuild": "cd dist && yarn install --production && zip -rq ../dist.zip ."
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.100",
    "@types/node": "^18.0.0",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "aws-sdk": "^2.1157.0",
    "axios": "^0.27.2"
  }
}