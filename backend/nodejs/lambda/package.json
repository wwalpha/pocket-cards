{
  "name": "lambda",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "run-p build:*",
    "build:cognito": "cd cognito && yarn install && yarn build",
    "build:start": "cd start && yarn install && yarn build",
    "build:stop": "cd stop && yarn install && yarn build",
    "build:status": "cd status && yarn install && yarn build",
    "build:authorizerv1": "cd authorizer_v1 && yarn install && yarn build",
    "build:authorizerv2": "cd authorizer_v2 && yarn install && yarn build",
    "build:vision": "cd vision && yarn install && yarn build",
    "build:wss_connect": "cd wss_connect && yarn install && yarn build",
    "build:wss_disconnect": "cd wss_disconnect && yarn install && yarn build",
    "build:wss_commands": "cd wss_commands && yarn install && yarn build",
    "build:wss_relay": "cd wss_relay && yarn install && yarn build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}