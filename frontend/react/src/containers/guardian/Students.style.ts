import { Theme } from '@mui/material/styles';
import { SxProps } from '@mui/system';

export const styles = {
  root: {
    display: 'flex',
    m: 2,
  } as SxProps<Theme>,
  users: {
    display: 'flex',
  } as SxProps<Theme>,
  infos: {
    display: 'flex',
  } as SxProps<Theme>,
  tools: {
    display: 'flex',
  } as SxProps<Theme>,
};
